/*
 * UniCourt Enterprise APIs
 * <button><a href=\"/enterpriseapi/download/UniCourt-Enterprise-API-Spec.yaml\" >Download UniCourt Enterprise APIs Specification</a></button> 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by Konfig (https://konfigthis.com).
 * Do not edit the class manually.
 */


package com.konfigthis.client.model;

import java.util.Objects;
import java.util.Arrays;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import com.konfigthis.client.model.Address;
import com.konfigthis.client.model.Email;
import com.konfigthis.client.model.Phone;
import io.swagger.annotations.ApiModel;
import io.swagger.annotations.ApiModelProperty;
import java.io.IOException;
import java.util.ArrayList;
import java.util.List;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import org.apache.commons.lang3.StringUtils;

import java.lang.reflect.Type;
import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Map.Entry;
import java.util.Set;

import com.konfigthis.client.JSON;

/**
 * Contact object data schema.
 */
@ApiModel(description = "Contact object data schema.")@javax.annotation.Generated(value = "Generated by https://konfigthis.com")
public class Contact {
  public static final String SERIALIZED_NAME_ADDRESS_ARRAY = "addressArray";
  @SerializedName(SERIALIZED_NAME_ADDRESS_ARRAY)
  private List<Address> addressArray = new ArrayList<>();

  public static final String SERIALIZED_NAME_EMAIL_ARRAY = "emailArray";
  @SerializedName(SERIALIZED_NAME_EMAIL_ARRAY)
  private List<Email> emailArray = new ArrayList<>();

  public static final String SERIALIZED_NAME_OBJECT = "object";
  @SerializedName(SERIALIZED_NAME_OBJECT)
  private String _object = "Contact";

  public static final String SERIALIZED_NAME_PHONE_NUMBER_ARRAY = "phoneNumberArray";
  @SerializedName(SERIALIZED_NAME_PHONE_NUMBER_ARRAY)
  private List<Phone> phoneNumberArray = new ArrayList<>();

  public Contact() {
  }

  public Contact addressArray(List<Address> addressArray) {
    
    
    
    
    this.addressArray = addressArray;
    return this;
  }

  public Contact addAddressArrayItem(Address addressArrayItem) {
    this.addressArray.add(addressArrayItem);
    return this;
  }

   /**
   * List of available addresses.
   * @return addressArray
  **/
  @javax.annotation.Nullable
  @ApiModelProperty(required = true, value = "List of available addresses.")

  public List<Address> getAddressArray() {
    return addressArray;
  }


  public void setAddressArray(List<Address> addressArray) {
    
    
    
    this.addressArray = addressArray;
  }


  public Contact emailArray(List<Email> emailArray) {
    
    
    
    
    this.emailArray = emailArray;
    return this;
  }

  public Contact addEmailArrayItem(Email emailArrayItem) {
    this.emailArray.add(emailArrayItem);
    return this;
  }

   /**
   * List of available emails.
   * @return emailArray
  **/
  @javax.annotation.Nullable
  @ApiModelProperty(required = true, value = "List of available emails.")

  public List<Email> getEmailArray() {
    return emailArray;
  }


  public void setEmailArray(List<Email> emailArray) {
    
    
    
    this.emailArray = emailArray;
  }


  public Contact _object(String _object) {
    
    
    if (_object != null && _object.length() < 7) {
      throw new IllegalArgumentException("Invalid value for _object. Length must be greater than or equal to 7.");
    }
    
    this._object = _object;
    return this;
  }

   /**
   * Get _object
   * @return _object
  **/
  @javax.annotation.Nonnull
  @ApiModelProperty(example = "Contact", required = true, value = "")

  public String getObject() {
    return _object;
  }


  public void setObject(String _object) {
    
    
    if (_object != null && _object.length() < 7) {
      throw new IllegalArgumentException("Invalid value for _object. Length must be greater than or equal to 7.");
    }
    this._object = _object;
  }


  public Contact phoneNumberArray(List<Phone> phoneNumberArray) {
    
    
    
    
    this.phoneNumberArray = phoneNumberArray;
    return this;
  }

  public Contact addPhoneNumberArrayItem(Phone phoneNumberArrayItem) {
    this.phoneNumberArray.add(phoneNumberArrayItem);
    return this;
  }

   /**
   * List of available phone numbers.
   * @return phoneNumberArray
  **/
  @javax.annotation.Nullable
  @ApiModelProperty(required = true, value = "List of available phone numbers.")

  public List<Phone> getPhoneNumberArray() {
    return phoneNumberArray;
  }


  public void setPhoneNumberArray(List<Phone> phoneNumberArray) {
    
    
    
    this.phoneNumberArray = phoneNumberArray;
  }

  /**
   * A container for additional, undeclared properties.
   * This is a holder for any undeclared properties as specified with
   * the 'additionalProperties' keyword in the OAS document.
   */
  private Map<String, Object> additionalProperties;

  /**
   * Set the additional (undeclared) property with the specified name and value.
   * If the property does not already exist, create it otherwise replace it.
   *
   * @param key name of the property
   * @param value value of the property
   * @return the Contact instance itself
   */
  public Contact putAdditionalProperty(String key, Object value) {
    if (this.additionalProperties == null) {
        this.additionalProperties = new HashMap<String, Object>();
    }
    this.additionalProperties.put(key, value);
    return this;
  }

  /**
   * Return the additional (undeclared) property.
   *
   * @return a map of objects
   */
  public Map<String, Object> getAdditionalProperties() {
    return additionalProperties;
  }

  /**
   * Return the additional (undeclared) property with the specified name.
   *
   * @param key name of the property
   * @return an object
   */
  public Object getAdditionalProperty(String key) {
    if (this.additionalProperties == null) {
        return null;
    }
    return this.additionalProperties.get(key);
  }


  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    Contact contact = (Contact) o;
    return Objects.equals(this.addressArray, contact.addressArray) &&
        Objects.equals(this.emailArray, contact.emailArray) &&
        Objects.equals(this._object, contact._object) &&
        Objects.equals(this.phoneNumberArray, contact.phoneNumberArray)&&
        Objects.equals(this.additionalProperties, contact.additionalProperties);
  }

  @Override
  public int hashCode() {
    return Objects.hash(addressArray, emailArray, _object, phoneNumberArray, additionalProperties);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class Contact {\n");
    sb.append("    addressArray: ").append(toIndentedString(addressArray)).append("\n");
    sb.append("    emailArray: ").append(toIndentedString(emailArray)).append("\n");
    sb.append("    _object: ").append(toIndentedString(_object)).append("\n");
    sb.append("    phoneNumberArray: ").append(toIndentedString(phoneNumberArray)).append("\n");
    sb.append("    additionalProperties: ").append(toIndentedString(additionalProperties)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("addressArray");
    openapiFields.add("emailArray");
    openapiFields.add("object");
    openapiFields.add("phoneNumberArray");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
    openapiRequiredFields.add("addressArray");
    openapiRequiredFields.add("emailArray");
    openapiRequiredFields.add("object");
    openapiRequiredFields.add("phoneNumberArray");
  }

 /**
  * Validates the JSON Object and throws an exception if issues found
  *
  * @param jsonObj JSON Object
  * @throws IOException if the JSON Object is invalid with respect to Contact
  */
  public static void validateJsonObject(JsonObject jsonObj) throws IOException {
      if (jsonObj == null) {
        if (!Contact.openapiRequiredFields.isEmpty()) { // has required fields but JSON object is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in Contact is not found in the empty JSON string", Contact.openapiRequiredFields.toString()));
        }
      }

      // check to make sure all required properties/fields are present in the JSON string
      for (String requiredField : Contact.openapiRequiredFields) {
        if (jsonObj.get(requiredField) == null) {
          throw new IllegalArgumentException(String.format("The required field `%s` is not found in the JSON string: %s", requiredField, jsonObj.toString()));
        }
      }
      // ensure the json data is an array
      if (!jsonObj.get("addressArray").isJsonArray()) {
        throw new IllegalArgumentException(String.format("Expected the field `addressArray` to be an array in the JSON string but got `%s`", jsonObj.get("addressArray").toString()));
      }

      JsonArray jsonArrayaddressArray = jsonObj.getAsJsonArray("addressArray");
      // validate the required field `addressArray` (array)
      for (int i = 0; i < jsonArrayaddressArray.size(); i++) {
        Address.validateJsonObject(jsonArrayaddressArray.get(i).getAsJsonObject());
      };
      // ensure the json data is an array
      if (!jsonObj.get("emailArray").isJsonArray()) {
        throw new IllegalArgumentException(String.format("Expected the field `emailArray` to be an array in the JSON string but got `%s`", jsonObj.get("emailArray").toString()));
      }

      JsonArray jsonArrayemailArray = jsonObj.getAsJsonArray("emailArray");
      // validate the required field `emailArray` (array)
      for (int i = 0; i < jsonArrayemailArray.size(); i++) {
        Email.validateJsonObject(jsonArrayemailArray.get(i).getAsJsonObject());
      };
      if (!jsonObj.get("object").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format("Expected the field `object` to be a primitive type in the JSON string but got `%s`", jsonObj.get("object").toString()));
      }
      // ensure the json data is an array
      if (!jsonObj.get("phoneNumberArray").isJsonArray()) {
        throw new IllegalArgumentException(String.format("Expected the field `phoneNumberArray` to be an array in the JSON string but got `%s`", jsonObj.get("phoneNumberArray").toString()));
      }

      JsonArray jsonArrayphoneNumberArray = jsonObj.getAsJsonArray("phoneNumberArray");
      // validate the required field `phoneNumberArray` (array)
      for (int i = 0; i < jsonArrayphoneNumberArray.size(); i++) {
        Phone.validateJsonObject(jsonArrayphoneNumberArray.get(i).getAsJsonObject());
      };
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!Contact.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'Contact' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<Contact> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(Contact.class));

       return (TypeAdapter<T>) new TypeAdapter<Contact>() {
           @Override
           public void write(JsonWriter out, Contact value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             obj.remove("additionalProperties");
             // serialize additonal properties
             if (value.getAdditionalProperties() != null) {
               for (Map.Entry<String, Object> entry : value.getAdditionalProperties().entrySet()) {
                 if (entry.getValue() instanceof String)
                   obj.addProperty(entry.getKey(), (String) entry.getValue());
                 else if (entry.getValue() instanceof Number)
                   obj.addProperty(entry.getKey(), (Number) entry.getValue());
                 else if (entry.getValue() instanceof Boolean)
                   obj.addProperty(entry.getKey(), (Boolean) entry.getValue());
                 else if (entry.getValue() instanceof Character)
                   obj.addProperty(entry.getKey(), (Character) entry.getValue());
                 else {
                   obj.add(entry.getKey(), gson.toJsonTree(entry.getValue()).getAsJsonObject());
                 }
               }
             }
             elementAdapter.write(out, obj);
           }

           @Override
           public Contact read(JsonReader in) throws IOException {
             JsonObject jsonObj = elementAdapter.read(in).getAsJsonObject();
             validateJsonObject(jsonObj);
             // store additional fields in the deserialized instance
             Contact instance = thisAdapter.fromJsonTree(jsonObj);
             for (Map.Entry<String, JsonElement> entry : jsonObj.entrySet()) {
               if (!openapiFields.contains(entry.getKey())) {
                 if (entry.getValue().isJsonPrimitive()) { // primitive type
                   if (entry.getValue().getAsJsonPrimitive().isString())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsString());
                   else if (entry.getValue().getAsJsonPrimitive().isNumber())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsNumber());
                   else if (entry.getValue().getAsJsonPrimitive().isBoolean())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsBoolean());
                   else
                     throw new IllegalArgumentException(String.format("The field `%s` has unknown primitive type. Value: %s", entry.getKey(), entry.getValue().toString()));
                 } else if (entry.getValue().isJsonArray()) {
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), List.class));
                 } else { // JSON object
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), HashMap.class));
                 }
               }
             }
             return instance;
           }

       }.nullSafe();
    }
  }

 /**
  * Create an instance of Contact given an JSON string
  *
  * @param jsonString JSON string
  * @return An instance of Contact
  * @throws IOException if the JSON string is invalid with respect to Contact
  */
  public static Contact fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, Contact.class);
  }

 /**
  * Convert an instance of Contact to an JSON string
  *
  * @return JSON string
  */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

